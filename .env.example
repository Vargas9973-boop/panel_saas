# Mismo proyecto de Supabase que wing-house-web (repo WINGS-HOUSE-WEB) --
# son valores públicos (anon key), no secretos, así que se pueden copiar
# tal cual de ese repo.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
